LAGER: LAnguage for GEsture Representation
==========================================

Installation instructions:

1) Install all dependencies as explained in dependencies.txt
2) Compile and install the LAGER modules:
    ~$ cd lager
    ~$ ./buildall

